Reminders To-Do:
    - Favicon [/]
    - PHP [Edited: /]
        -Inquiries/Contact [/]
        -Feedback/Review [] - make into db
         ~ db created, view db placed (index.html -> index.php [copy]) [/]
         ~ form for submitting feedback []
     
Query:

CREATE TABLE clientfeedback (
    id INT(11) AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    rating INT NOT NULL,
    review_text TEXT NOT NULL,
    is_approved BOOLEAN DEFAULT FALSE,
    submitted_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

INSERT INTO clientfeedback (name, rating, feedback, is_approved) 
VALUES 
('James T.', 5, 'My car looks brand new again. The ceramic coating is worth every peso.', 1),
('Carla M.', 5, 'Great service and friendly staff. They really know how to take care of luxury cars.', 1),
('Ryan D.', 4, 'Highly recommended for PPF installation. The finish is flawless.', 1);